subprojects {
    group = 'me.dwtj.ex.openjdk.langtools.types'

    apply plugin: 'java'

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenLocal()
        mavenCentral()
    }
}
